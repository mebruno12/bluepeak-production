# Database - Supabase Connection
# Get this from: Supabase Dashboard -> Settings -> Database -> Connection string (Transaction mode)
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct Connection (for migrations) - Get from Connection Pooling -> Direct connection
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# JWT Secret for authentication - GENERATE A STRONG SECRET IN PRODUCTION!
# Generate with: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Base Path (for deployment at bluepeak.pt/calculadora-investimento-imobiliario)
# Leave empty for local development, set in Vercel environment variables
NEXT_PUBLIC_BASE_PATH=""

# Node Environment
NODE_ENV="development"
